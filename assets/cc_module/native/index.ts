import { JSB } from "cc/env";
import { AssetManager } from "./asset/AssetManager";
import { storage } from "./storage";

// cocos 本地的工具绑定
export const native = {

    AssetManager,

    storage,
    
}